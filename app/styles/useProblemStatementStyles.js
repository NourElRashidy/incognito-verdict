import { makeStyles } from '@material-ui/core/styles';

const useProblemStatementStyles = makeStyles((theme) => ({
  problemStatementContainer: {
    width: '95%',
    margin: 15,
    display: 'flex'
  }
}));

export default useProblemStatementStyles;
